version: "2"

linters:
  # 全ての linter を無効化
  default: none
  # 指定した linter のみ有効化
  enable:
    - govet
    - errcheck
    - staticcheck
    - revive
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

run:
  tests: true
